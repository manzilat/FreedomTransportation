@model FreedomTransportation.Models.Customer
@{
    ViewBag.Title = "Confirm Pickup";//You can add customer name to this variable string
}

<h2>@ViewBag.Title</h2>
<div>
    <div class="col-md-6">
        <h4>
            <strong>Confirm</strong>
        </h4>
    </div>
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Confirm Pickup" class="btn btn-default" />
        </div>
    }
</div>

<hr />
@Html.ActionLink("Back to Today's List", "DriversPickups")
